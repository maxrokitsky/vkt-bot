volumes:
  vk-bot-pg:

services:
  postgres-db:
    image: postgres:latest
    container_name: postgres-db
    volumes:
      - vk-bot-pg:/bitnami/postgresql
    environment:
      - POSTGRES_HOST_AUTH_METHOD=trust
    env_file: .env
    # expose:
    #   - "5432"
    ports:
      - "16432:5432"
